# Sudo command allowlist
# One command per line (without the "sudo" prefix).
# Blank lines and lines beginning with # are ignored.
# The entrypoint converts this file into /etc/sudoers Cmnd_Alias directives at startup,
# then makes /etc/sudoers immutable so the agent cannot modify sudo permissions.
#
# Example:
#   apt-get update
#   apt-get install -y curl
#
# This file is EMPTY by default — add entries only as needed.
